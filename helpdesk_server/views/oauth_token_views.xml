<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_oauth_token_list" model="ir.ui.view">
            <field name="name">oauth.token.list</field>
            <field name="model">oauth.token</field>
            <field name="arch" type="xml">
                <list string="OAuth Tokens"
                    decoration-muted="status != 'active'">
                    <field name="client_name" string="Client" />
                    <field name="token_id" string="JWT ID" />
                    <field name="scope" />
                    <field name="issued_at" />
                    <field name="expires_at" />
                    <field name="status" widget="badge"
                        decoration-success="status == 'active'"
                        decoration-warning="status == 'expired'"
                        decoration-danger="status == 'revoked'" />
                    <field name="usage_count" string="Uses" />
                    <field name="last_used" />
                    <button name="action_revoke" string="Revoke" type="object" icon="fa-ban"
                        invisible="status != 'active'"
                        confirm="Revoke this token?" />
                </list>
            </field>
        </record>

        <record id="view_oauth_token_form" model="ir.ui.view">
            <field name="name">oauth.token.form</field>
            <field name="model">oauth.token</field>
            <field name="arch" type="xml">
                <form string="OAuth Token">
                    <header>
                        <button name="action_revoke" string="Revoke Token"
                            type="object" class="oe_highlight"
                            confirm="Are you sure you want to revoke this token?"
                            invisible="status != 'active'" />
                        <field name="status" widget="statusbar"
                            statusbar_visible="active,expired,revoked" />
                    </header>
                    <sheet>
                        <group>
                            <group string="Token Information">
                                <field name="client_id" readonly="1" />
                                <field name="token_id" readonly="1" />
                                <field name="token_type" readonly="1" />
                                <field name="scope" readonly="1" />
                            </group>
                            <group string="Validity">
                                <field name="issued_at" readonly="1" />
                                <field name="expires_at" readonly="1" />
                                <field name="last_used" readonly="1" />
                                <field name="usage_count" readonly="1" />
                            </group>
                        </group>
                        <group string="Revocation" invisible="revoked == False">
                            <field name="revoked_at" readonly="1" />
                            <field name="revoked_by" readonly="1" />
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="view_oauth_token_search" model="ir.ui.view">
            <field name="name">oauth.token.search</field>
            <field name="model">oauth.token</field>
            <field name="arch" type="xml">
                <search string="OAuth Tokens">
                    <field name="client_name" />
                    <field name="token_id" />
                    <filter string="Active" name="f_active"
                        domain="[('revoked','=',False),('expires_at','>',datetime.datetime.now())]" />
                    <filter string="Revoked" name="f_revoked" domain="[('revoked','=',True)]" />
                    <separator />
                    <!-- <group string="Group By">
                        <filter string="Client" name="by_client" context="{'group_by': 'client_id'}" />
                    </group> -->
                </search>
            </field>
        </record>

        <record id="action_oauth_token" model="ir.actions.act_window">
            <field name="name">OAuth Tokens</field>
            <field name="res_model">oauth.token</field>
            <field name="view_mode">list,form</field>
            <field name="context">{'search_default_f_active': 1}</field>
        </record>

    </data>
</odoo>